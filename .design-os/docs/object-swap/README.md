# Object Swap Runs

`tools/object-swap-check.mjs` writes local object-swap evidence here.

Generated run folders are ignored by git. Use this for rosters, galleries, configurators, and character-selection apps where different objects can break framing.
