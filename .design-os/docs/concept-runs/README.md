# Concept Runs

`tools/capture-concepts.mjs` writes local rendered concept screenshots here.

Generated concept run folders are ignored by git. Text-only concepts fail; concept routes must be rendered and captured at `390 / 768 / 1440`.
