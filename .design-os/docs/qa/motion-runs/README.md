# Motion Runs

Local motion-sequence capture reports from `tools/capture-motion-sequence.mjs` are written here.

Generated run folders should contain:

- `motion-sequence-report.json`
- `motion-sequence-report.md`
- `screenshots/` when Playwright capture succeeds

If Playwright is unavailable or browser launch fails, the tool writes a blocked report instead of fake screenshots.

