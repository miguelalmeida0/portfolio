# scroll to assemble cards

Scattered cards align into ordered containers. Define starting offsets, settle positions, and final reading order.

Required: route map, chapter map, moving object inventory, reduced-motion fallback, mobile fallback, and QA evidence.
